#server:
#  servlet:
#    context-path: /content
#  port: 63040

spring:
  profiles:
    active: dev
  application:
    name: content-api
  main:
    banner-mode: off
  cloud:
    nacos:
      server-addr: 47.96.91.58:8848
      discovery:
        namespace: dev
        group: xuecheng-plus-project
      config:
        namespace: dev
        group: xuecheng-plus-project
        file-extension: yaml
        refresh-enabled: true
#         引用content-service config
        extension-configs:
          - data-id: content-service-${spring.profiles.active}.yaml
            group: xuecheng-plus-project
            refresh: true
        shared-configs:
          - data-id: swagger-${spring.profiles.active}.yaml
            group: xuecheng-plus-common
            refresh: true
          - data-id: logging-${spring.profiles.active}.yaml
            group: xuecheng-plus-common
            refresh: true
